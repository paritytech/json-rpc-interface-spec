# Introduction

The `statement` functions allow submitting and subscribing to signed statements in the node's statement store. The statement store is a decentralized key-value store for authenticated statements that are propagated over the peer-to-peer network.
